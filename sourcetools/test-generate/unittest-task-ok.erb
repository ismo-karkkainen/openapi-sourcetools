<%=
require_relative '../../shared/unittest'

out = 'default'

assert(defined?(Gen).nil?, false, 'Gen defined')
unless Gen.t.nil? || Gen.t.x.nil?
  Gen.t.name = Gen.t.x['out']
  out = Gen.t.x['out']
end
out
%>
